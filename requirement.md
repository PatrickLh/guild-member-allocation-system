# 需求描述

本项目是针对游戏中工会人员每周组队进行对抗的这一过程，通过录入工会人员信息，进行快速分组，需要满足以下要求：

## 1、人员信息的维护
每次开始分组前，需要维护工会人员的名称/职业/战力的信息，其中职业有四类：斗士、术士、骑士、贤者，将维护的信息存储为本地文件（不使用数据库，因为信息量少），每次需要读取该信息，并支持内容的增减（用户ID自动生成）

## 2、 第一组人员的选定
手动选定n(n < 4)个人员作为第一组人员，其余第一组人员按照之后的自动分组规则进行，并持久化记录本次手动设定的人员(由于该目的是希望在所有人都有机会轮次到第一组，所有人都轮派过第一组以后，又重新开始分组)

## 3、自动分组
* 每组4个人，不足4个人的留空，需要注意第一组的分配规则
* 分组按照战力从高到低进行分组
* 如果战力值相差10%以内，优先选用不同职业，例如：如果某一组已经有斗士职业，如果按照战力下一个人员是斗士100w，贤者92w，则优先考虑贤者

## 4、手动调整
可以对自动生成后的结果进行调整，通过选择人员名字，自动调整选择人和被选择人所在分组

# 输出结果

根据以上规则，展示最后的分组名单，由于该功能在移动端上使用，需要考虑移动端适配

# 其他
项目尽量简单，考虑到可能会使用读取本地文件的能力，如果需要使用后端服务，可以编写python应用来处理，前端服务使用标准的html即可

如果有不清楚的地方或者需要了解的，可随时给我进行沟通